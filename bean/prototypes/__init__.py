"""Prototype connectors — reference implementations spanning every API shape (REST, GraphQL,
POST-list, OAuth-CLI, file, chat week-digest, whole-collection). They are NOT registered by
default. Enable one by name in the global config (`plugins.prototypes`) or copy it into
~/.bean/plugins/ as a starting point for your own. See `docs/authoring-connectors.md` and
`bean/prototypes/registry.py` for the one-row registration each one needs."""
